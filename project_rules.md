# Oli 项目规则

## 01. 启动程序指令约定

当用户输入以下关键词时：
- `启动程序`
- `启动`
- `打开`
- `英语打卡`

**默认执行以下动作（无需二次询问）：**

1.  **检查环境**：进入目录 `/Users/lucas/Work/AntiGravity/Oli/English_Study_Plan`。
2.  **启动服务**：使用本地虚拟环境启动 Flask 应用。
    - 命令：`nohup ./venv/bin/python app.py > server.log 2>&1 & echo $! > server.pid`
    - 端口：`8888`
3.  **打开浏览器**：自动访问 [http://127.0.0.1:8888](http://127.0.0.1:8888) 并确认页面加载正常。

## 02. 项目背景

- **项目名称**：Oli English Study Plan (英语打卡)
- **核心功能**：中考词汇突击、听力练习、完形填空实战。
- **用户标识**：Oli (魔导士)

## 03. 自动课程扩充（改造五）

每次启动时，在打开浏览器之前，执行以下检查：

1. 读取 `vocab_progress.json` 中的 `completedDays` 最大值
2. 读取 `listening_data.json` 和 `cloze_data.json` 中的最大 `day` 值
3. 如果 `max_data_day - max_completed_day < 2`，自动扩充后续天数
4. 扩充标准：北京中考英语难度（A2-B1），每天生成 1 篇听力 + 1 篇完形

## 04. 学习效果改造规则（2026-04 实施）

已实施以下改造以确保 Oli 真正掌握词汇：

- **复习门槛 (Review Gate)**：错题 ≥15 且 2天未复习，或 ≥30 个，锁定新关
- **加权题型**：新词 70% 选择题；熟悉词 80% 产出型（拼写/填空/听写）
- **错题消化引擎**：Leitner Box 达到 3 才移出错题本（连续多次答对）
- **三合一通关**：词汇+听力+完形都完成才显示🏆

---
*注：本文件旨在为 AI 助手提供持久化记忆，确保在不同会话中指令的一致性。*
